

### 整体概览

- **技术栈**
  - **Vue 3 + TypeScript**：`vue@3.5`，组合式 API + `<script setup>`.
  - **构建工具**：`vite`，脚手架非常清爽。
  - **状态管理**：`pinia`，在 `src/stores/modules` 下拆成 `song`、`user` 两个模块。
  - **路由**：`vue-router@4`，集中在 `src/router` 下，带路由守卫。
  - **UI 库**：`element-plus`（中文语言包）+ TailwindCSS + 自定义现代风格样式。
  - **测试/质量**：`vitest` + `eslint` + `prettier` + `vue-tsc` 类型检查。

---

### 启动入口 & 根组件

- **入口文件 `src/main.ts`**
  - 引入全局样式 `assets/main.css`、Tailwind、Element Plus 以及图标。
  - 创建 Vue 应用，挂载 `pinia`、`router`、`ElementPlus`，最后 `mount('#app')`。
- **根组件 `src/App.vue`**
  - 外层用 `ErrorBoundary` 包一层，内部就是 `<router-view>`。
  - 所有页面都跑在错误边界内部，方便兜底处理异常。

---

### 路由结构

- **`src/router/index.ts`**
  - `createRouter + createWebHistory` 创建 SPA 路由。
  - 路由配置从 `./modules/router.ts` 引入，并在这里调用 `setupGuards(router)` 注册守卫（例如登录校验、播放页访问控制等，具体逻辑在 `modules/guards.ts`）。
- **页面目录 `src/views`**
  - `home/index.vue`：首页推荐/榜单/播放队列。
  - `login/index.vue`：登录页。
  - `search/index.vue` + `search/components/SearchSongList.vue`：搜索结果页。
  - `play-page/index.vue`：大屏歌词播放页。
  - `playlistPage/index.vue` + `component/PlaylistItem.vue`：播放列表页。
  - `error/404.vue`：404 页面。

---

### 布局 & 播放相关整体结构

- **主布局 `src/layouts/PlayerLayout.vue`**
  - **左侧固定侧边栏**：LOGO + 简单菜单（首页、播放）。
  - **顶部固定搜索栏**：
    - 复杂的搜索体验：历史记录、热搜榜、搜索联想（`SearchAPI` 请求、节流、防抖）。
    - 响应 `Enter` 搜索，跳转到 `name: 'search'` 路由，并记录搜索历史到 `localStorage`。
    - 热搜 / 榜单区块使用 `RankBoardCard` 卡片展示“古风榜/摇滚榜/民谣榜”，并提供“播放全部”功能，直接改 `SongStore.playList`。
  - **中间主内容区域**：固定布局，用 `<router-view />` 渲染各页面，带自定义滚动条。
  - **中层**：`PlayPage` 播放详情页（从 `SongStore.songDetailsDisplay` 控制显隐），使用过渡动画并锁定 body 滚动。
  - **顶层**：底部 `PlayerControlBar` 永远固定在最上，叠在页面之上。
  - **播放列表弹层**：`PlayListPage` 通过 `SongStore.playListDisplay` 控制展示。

- **播放详情页 `src/views/play-page/index.vue`**
  - 左侧：专辑圆形封面，会根据 `SongStore.playStatus` 持续旋转，切歌会把角度重置。
  - 右侧：歌词滚动区域：
    - 使用 `SongStore.lyric` + `SongStore.currentTimeLyric` 来定位当前行。
    - 通过一系列 `ref` + `watch` 计算“当前播放行”和“居中行”，自动让当前播放句居中滚动。
    - 支持用户手动滚动：中线位置显示一个时间按钮，点击后通过 `SongStore.$patch` 更新播放时间，并让播放器从这句开始播。
    - 有细致的“暂停自动滚动 / 一段时间后恢复自动滚动”的逻辑，避免用户刚手动拖动就被系统滚回去。

---

### 首页逻辑（`src/views/home/index.vue`）

- 主要依赖 `useSongStore`：
  - `featuredSongs`：从 `playList` 里取前 6 首做“本周热播”。
  - `heroSong`：优先取当前播放歌曲，否则用 `featuredSongs[0]` 作为右侧大卡片。
  - `sideList`：侧边小列表使用播放队列前 8 首。
- 提供了几个本地格式化函数：
  - `formatArtists` 把歌手数组转为“歌手1 / 歌手2”。
  - `formatDuration` 把毫秒转成 `mm:ss`。
- UI 上是一个相当现代的 Landing + 推荐 + 排队列表布局，结合 Tailwind 类和自定义 CSS，风格偏玻璃拟态。

---

### 状态管理（Pinia）

- **导出统一入口：`src/stores/index.ts`**
  - `export { useUserStore } from './modules/user.ts'`
  - `export { useSongStore } from './modules/song.ts'`
- **核心：`src/stores/modules/song.ts`（建议你重点看一下）**
  - 负责：
    - 当前歌曲 `currentSong`、播放列表 `playList`、播放状态 `playStatus`。
    - 歌词 `lyric` + 当前时间对应的歌词 `currentTimeLyric`。
    - 播放进度条、播放模式、是否展示歌词页/播放列表页等 UI 状态。
  - 这里会和 `usePlayer` 钩子、`player-control-bar` 组件强关联。
- **用户：`src/stores/modules/user.ts`**
  - 一般会保存 `token`、用户信息、登录状态等（可配合 `api/modules/login.ts` 和路由守卫使用）。

---

### 业务 API 层

- **统一入口 `src/api/index.ts`**
  - 对外导出诸如 `SearchAPI`、`SongAPI`、`UserAPI` 等（从 `modules` 目录聚合）。
- **通用请求封装：`src/api/request.ts`**
  - 基于 `axios`，会做：
    - 实例化、baseURL / 超时设置。
    - 请求拦截：加 token、公共 header 等。
    - 响应拦截：处理错误码、统一错误提示。
- **分模块接口 `src/api/modules/*.ts`**
  - `login.ts`：登录相关请求。
  - `search.ts`：搜索建议 / 热搜 / 搜索结果等。
  - `song.ts`：获取歌曲详情、歌词等。
  - `user.ts`：用户信息、收藏等。

---

### 组合式 Hooks

- **`src/hooks/usePlayer.ts`**
  - 封装和音频播放相关的逻辑：播放/暂停、上一首、下一首、进度条拖动、播放模式等。
  - 会直接操作 `useSongStore`，并暴露控制方法给 `PlayerControlBar`、`ProgressBar` 等组件。
- **`src/hooks/useTheme.ts`**
  - 管理全局主题（暗色 / 浅色、跟随系统等），可能关联 Tailwind 的 `dark` class 或 body 上的主题 class。
- **`src/hooks/useUser.ts`**
  - 辅助用户登录/退出、获取用户信息，简化在各页面中操作 `useUserStore` 的逻辑。

---

### 播放控件组件

- **`src/components/player-control-bar/index.vue`**
  - 底部整体控制条容器。
  - 内部会组合：
    - `player-control/PlaybackControls.vue`：播放/暂停、上一首/下一首、播放模式按钮。
    - `player-control/ProgressBar.vue`：进度条（拖拽更新 `SongStore.currentTime`）。
    - `player-control/VolumeControl.vue`：音量调节。
    - `player-control/Collect.vue`：收藏按钮之类的交互。
- 各子组件通常只关心 UI 和事件，真正的播放逻辑由 `usePlayer` + `SongStore` 驱动。

---

### 工具 & 类型

- **工具函数 `src/utils`**
  - `format.ts`：如 `formatProgress` 等时间/进度格式化函数，歌词页、进度条都会用到。
  - `fun.ts`：杂项工具函数（节流、防抖、随机数等）。
  - `storage.ts`：对 `localStorage/sessionStorage` 的封装，用于历史记录、token 本地存储等。
- **类型定义 `src/types`**
  - `api/`：接口返回的 DTO 类型。
  - `player.d.ts`：播放器相关的 `Song`、歌词行等类型。
  - `search.d.ts`：搜索联想、热搜条目的类型。
  - `axios.d.ts`：对 axios 的类型扩展。

---

### 建议你熟悉代码的阅读顺序

1. **从整体流转开始**  
   - 看 `main.ts` → `App.vue` → `router/modules/router.ts`，了解有哪些路由页面以及公共布局（`PlayerLayout.vue`）。
2. **熟悉播放数据结构**  
   - 打开 `stores/modules/song.ts` 和 `types/player.d.ts`，看 `Song`、歌词行结构、`playStatus` 等字段。
3. **看播放 UI 如何驱动数据**  
   - `components/player-control-bar` 下几个组件 + `hooks/usePlayer.ts`。
   - `views/play-page/index.vue` 的歌词联动逻辑。
4. **看搜索与首页**  
   - `views/home/index.vue` + `layouts/PlayerLayout.vue` 中搜索相关逻辑。
   - `api/modules/search.ts` + `types/search.d.ts`，理解搜索接口数据结构。
5. **最后再看登录 / 用户 / 守卫**  
   - `views/login/index.vue`、`stores/modules/user.ts`、`router/modules/guards.ts`、`api/modules/login.ts`。

